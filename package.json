{
  "name": "prodesiginity",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./frontend\" --filter \"./dashboard\" run dev",
    "dev:frontend": "pnpm --filter prodesignity dev",
    "dev:dashboard": "pnpm --filter prodesignity-dashboard dev",
    "dev:backend": "pnpm --filter prodesignity-api dev",
    "build": "pnpm -r --filter \"./frontend\" --filter \"./dashboard\" run build",
    "build:dashboard": "pnpm --filter prodesignity-dashboard build",
    "build:backend": "pnpm --filter prodesignity-api build",
    "lint": "pnpm --filter prodesignity lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "prisma",
      "@prisma/engines",
      "unrs-resolver"
    ]
  }
}
