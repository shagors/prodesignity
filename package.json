{
  "name": "prodesiginity",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./frontend\" --filter \"./backend\" run dev",
    "dev:frontend": "pnpm --filter prodesignity dev",
    "dev:backend": "pnpm --filter prodesignity-api dev",
    "build": "pnpm -r --filter \"./frontend\" --filter \"./backend\" run build",
    "lint": "pnpm --filter prodesignity lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "unrs-resolver",
      "@prisma/engines",
      "prisma"
    ]
  }
}
